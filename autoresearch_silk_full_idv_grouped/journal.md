# Grouped Silk Autoresearch Journal

Task: predict `[toughness, E, strength, strain]` from a set of independently embedded spidroin
sequences per `idv`.

Baseline to run first:

```bash
python run_experiment.py --tag baseline
```

Record each hypothesis, result, and follow-up here.
