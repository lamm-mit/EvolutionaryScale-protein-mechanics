# Research journal

Append-only log of hypotheses, experiments, and what was learned (especially **negative** results,
so they aren't repeated). Newest at the bottom.

Start by running the baseline (`python run_experiment.py --tag baseline`) to establish the bar on the
current dataset (default `lamm-mit/silkome-masp`), then record it here as the first entry.

---
